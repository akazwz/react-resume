import Button from '@material-ui/core/Button';

const Resume = () => {
    return (
        <div>
            this is resume.
            <Button>btn</Button>
        </div>
    );
}

export default Resume;